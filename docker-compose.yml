version: "3.9"

services:
  medscripe:
    image: virajag/medscribe:latest
    container_name: medscripe_app
    ports:
      - "5000:5000"

    env_file:
      - .env

#    volumes:
#      - .:/app

    restart: unless-stopped